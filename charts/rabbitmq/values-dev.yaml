# rabbitmq - dev overrides. Only values that differ from values.yaml.
environment: dev
namespace: roboshop-dev

image:
  tag: "3-management-alpine"

persistence:
  storageClass: gp3
  size: 2Gi

nodeSelector: {}

commonAnnotations:
  argocd.argoproj.io/sync-wave: "0"
  roboshop.io/environment: "dev"
