# rabbitmq - production overrides. Only values that differ from values.yaml.
environment: production
namespace: roboshop-production

image:
  tag: "3-management-alpine"
  pullPolicy: IfNotPresent

persistence:
  storageClass: gp3
  size: 50Gi

nodeSelector:
  workload: roboshop
tolerations:
  - key: workload
    operator: Equal
    value: roboshop
    effect: NoSchedule

commonAnnotations:
  argocd.argoproj.io/sync-wave: "0"
  roboshop.io/environment: "production"
