apiVersion: v2
name: platform
description: Roboshop shared platform layer - namespace, quotas, limits, storage class, shared config, secrets integration, ALB ingress and baseline network policies
type: application
version: 1.0.0
appVersion: "1.0.0"
dependencies:
  - name: common
    version: 1.0.0
    repository: "file://../common"
